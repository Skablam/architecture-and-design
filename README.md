# Architecture and Design

This application is trying out a sphinx application using markdown to record architecture and design for projects.

To install this use the following:

```
pip install -r requirements.txt
```

To run this use this:

```
sphinx-autobuild . _build
```
